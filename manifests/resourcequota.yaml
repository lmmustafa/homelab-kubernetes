apiVersion: v1
kind: ResourceQuota
metadata:
  namespace: hom
  name: hom-resourcequota
spec:
  hard:
    pods: "3"
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi

    